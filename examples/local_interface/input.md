# Paper Figure Request

Draw a full-chain RAG system diagram suitable for a methods section.

Requirements:
- Use grouped layers (access, retrieval, generation, ops).
- Show interactions among router, retriever, vector DB, BM25, graph store, reranker, LLM, guardrails, and observability.
- Keep labels readable and avoid line/text occlusion.
- Export PNG for paper draft and SVG/TikZ for post-editing.
